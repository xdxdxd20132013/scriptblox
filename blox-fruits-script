local ScreenGui = Instance.new("ScreenGui")
local MainFrame = Instance.new("Frame")
local Title = Instance.new("TextLabel")
local ToggleMenuBtn = Instance.new("TextButton")

-- Перемикачі вкладок (3 розділи)
local CheatsTabBtn = Instance.new("TextButton")
local VisualTabBtn = Instance.new("TextButton")
local SettingsTabBtn = Instance.new("TextButton")

-- Контейнери для розділів
local CheatsFrame = Instance.new("Frame")
local VisualFrame = Instance.new("Frame")
local SettingsFrame = Instance.new("Frame")

-- Елементи вкладки «Чіти»
local ChestBtn = Instance.new("TextButton")
local LevelBtn = Instance.new("TextButton")
local FlyBtn = Instance.new("TextButton")

-- Елементи вкладки «Візуал»
local DayBtn = Instance.new("TextButton")
local NightBtn = Instance.new("TextButton")
local DefaultTimeBtn = Instance.new("TextButton")
local FruitEspBtn = Instance.new("TextButton")

-- Елементи вкладки «Налаштування»
local SpeedTitle = Instance.new("TextLabel")
local SpeedInput = Instance.new("TextBox")
local SetSpeedBtn = Instance.new("TextButton")
local ExitBtn = Instance.new("TextButton")

local Player = game:GetService("Players").LocalPlayer
local Lighting = game:GetService("Lighting")
ScreenGui.Parent = game:GetService("CoreGui") or Player:WaitForChild("PlayerGui")
ScreenGui.ResetOnSpawn = false
-- КНОПКА-КВАДРАТИК ON/OFF (ЗГОРТАННЯ)
ToggleMenuBtn.Name = "XenoToggleBtn"
ToggleMenuBtn.Parent = ScreenGui
ToggleMenuBtn.Position = UDim2.new(0.01, 0, 0.3, 0)
ToggleMenuBtn.Size = UDim2.new(0, 45, 0, 45)
ToggleMenuBtn.BackgroundColor3 = Color3.fromRGB(39, 174, 96)
ToggleMenuBtn.Text = "ON"
ToggleMenuBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
ToggleMenuBtn.Font = Enum.Font.SourceSansBold
ToggleMenuBtn.TextSize = 14
Instance.new("UICorner", ToggleMenuBtn).CornerRadius = UDim.new(0, 8)
ToggleMenuBtn.Active = true
ToggleMenuBtn.Draggable = true

-- ГОЛОВНЕ ВІКНО МЕНЮ
MainFrame.Name = "BF_Xeno_Tabbed_Menu"
MainFrame.Parent = ScreenGui
MainFrame.BackgroundColor3 = Color3.fromRGB(15, 15, 20)
MainFrame.Position = UDim2.new(0.05, 0, 0.3, 0)
MainFrame.Size = UDim2.new(0, 240, 0, 220)
MainFrame.Active = true
MainFrame.Draggable = true
MainFrame.Visible = true
Instance.new("UICorner", MainFrame).CornerRadius = UDim.new(0, 10)

Title.Parent = MainFrame
Title.BackgroundTransparency = 1
Title.Size = UDim2.new(1, 0, 0, 30)
Title.Text = "Blox Fruits Perfect [Xeno]"
Title.TextColor3 = Color3.fromRGB(255, 255, 255)
Title.TextSize = 14
Title.Font = Enum.Font.SourceSansBold
local function styleTabSelect(btn, text, xPos)
    btn.Parent = MainFrame
    btn.Position = UDim2.new(xPos, 0, 0.16, 0)
    btn.Size = UDim2.new(0.29, 0, 0, 26)
    btn.BackgroundColor3 = Color3.fromRGB(30, 30, 35)
    btn.Text = text
    btn.TextColor3 = Color3.fromRGB(200, 200, 200)
    btn.TextSize = 12
    btn.Font = Enum.Font.SourceSansBold
    Instance.new("UICorner", btn).CornerRadius = UDim.new(0, 5)
end

styleTabSelect(CheatsTabBtn, "⚔️ Чіти", 0.04)
styleTabSelect(VisualTabBtn, "👁️ Візуал", 0.355)
styleTabSelect(SettingsTabBtn, "⚙️ Налашт.", 0.67)
CheatsTabBtn.BackgroundColor3 = Color3.fromRGB(45, 45, 55)

-- СИСТЕМА АВТО-КОНТЕЙНЕРІВ (UIListLayout)
local function createTabFrame(f, name, visible)
    f.Name = name f.Parent = MainFrame f.BackgroundTransparency = 1
    f.Position = UDim2.new(0, 0, 0.32, 0) f.Size = UDim2.new(1, 0, 0.68, 0) f.Visible = visible
    local layout = Instance.new("UIListLayout", f)
    layout.SortOrder = Enum.SortOrder.LayoutOrder layout.Padding = UDim.new(0, 5)
    local padding = Instance.new("UIPadding", f)
    padding.PaddingTop = UDim.new(0, 4) padding.PaddingLeft = UDim.new(0, 10) padding.PaddingRight = UDim.new(0, 10)
end

createTabFrame(CheatsFrame, "CheatsSection", true)
createTabFrame(VisualFrame, "VisualSection", false)
createTabFrame(SettingsFrame, "SettingsSection", false)
local function styleButton(btn, parent, text, color)
    btn.Parent = parent btn.Size = UDim2.new(1, 0, 0, 28)
    btn.BackgroundColor3 = color or Color3.fromRGB(45, 45, 55) btn.Text = text
    btn.TextColor3 = Color3.fromRGB(255, 255, 255) btn.TextSize = 12 btn.Font = Enum.Font.SourceSansBold
    Instance.new("UICorner", btn).CornerRadius = UDim.new(0, 5)
end

styleButton(ChestBtn, CheatsFrame, "Auto Chest: OFF")
styleButton(LevelBtn, CheatsFrame, "Auto Level (Не працює)")
styleButton(FlyBtn, CheatsFrame, "Fly Mode: OFF")

styleButton(DayBtn, VisualFrame, "☀️ Особистий День", Color3.fromRGB(230, 126, 34))
styleButton(NightBtn, VisualFrame, "🌙 Особиста Ніч", Color3.fromRGB(41, 128, 185))
styleButton(DefaultTimeBtn, VisualFrame, "⌛ Класичний Час", Color3.fromRGB(39, 174, 96))
styleButton(FruitEspBtn, VisualFrame, "🍓 Fruit Detector: OFF", Color3.fromRGB(142, 68, 173))

SpeedTitle.Parent = SettingsFrame SpeedTitle.BackgroundTransparency = 1 SpeedTitle.Size = UDim2.new(1, 0, 0, 15)
SpeedTitle.Text = "Швидкість польоту:" SpeedTitle.TextColor3 = Color3.fromRGB(200, 200, 200) SpeedTitle.TextSize = 12

local SpeedRow = Instance.new("Frame", SettingsFrame) SpeedRow.BackgroundTransparency = 1 SpeedRow.Size = UDim2.new(1, 0, 0, 26)
local rowLayout = Instance.new("UIListLayout", SpeedRow) rowLayout.FillDirection = Enum.FillDirection.Horizontal rowLayout.Padding = UDim.new(0, 8)

SpeedInput.Parent = SpeedRow SpeedInput.Size = UDim2.new(0.45, 0, 1, 0) SpeedInput.BackgroundColor3 = Color3.fromRGB(25, 25, 30)
SpeedInput.Text = "70" SpeedInput.TextColor3 = Color3.fromRGB(255, 255, 255) Instance.new("UICorner", SpeedInput).CornerRadius = UDim.new(0, 5)

styleButton(SetSpeedBtn, SpeedRow, "Зберегти", Color3.fromRGB(52, 73, 94)) SetSpeedBtn.Size = UDim2.new(0.5, 0, 1, 0)
styleButton(ExitBtn, SettingsFrame, "STOP & DESTROY CHEAT", Color3.fromRGB(192, 57, 43))

_G.ChestFarm = false _G.FlyMode = false _G.FlySpeed = 70 _G.LocalVisualTime = "Default" _G.FruitEsp = false
local currentTween, currentBV = nil, nil
local function stopMovement() if currentTween then currentTween:Cancel() currentTween = nil end if currentBV then currentBV:Destroy() currentBV = nil end end
-- ЛОГІКА ЗГОРТАННЯ МЕНЮ ЧЕРЕЗ КВАДРАТИК
ToggleMenuBtn.MouseButton1Click:Connect(function()
    MainFrame.Visible = not MainFrame.Visible
    ToggleMenuBtn.Text = MainFrame.Visible and "ON" or "OFF"
    ToggleMenuBtn.BackgroundColor3 = MainFrame.Visible and Color3.fromRGB(39, 174, 96) or Color3.fromRGB(192, 57, 43)
end)

DayBtn.MouseButton1Click:Connect(function() _G.LocalVisualTime = "Day" end)
NightBtn.MouseButton1Click:Connect(function() _G.LocalVisualTime = "Night" end)
DefaultTimeBtn.MouseButton1Click:Connect(function() _G.LocalVisualTime = "Default" end)

CheatsTabBtn.MouseButton1Click:Connect(function() CheatsFrame.Visible, VisualFrame.Visible, SettingsFrame.Visible = true, false, false end)
VisualTabBtn.MouseButton1Click:Connect(function() CheatsFrame.Visible, VisualFrame.Visible, SettingsFrame.Visible = false, true, false end)
SettingsTabBtn.MouseButton1Click:Connect(function() CheatsFrame.Visible, VisualFrame.Visible, SettingsFrame.Visible = false, false, true end)

-- ОПТИМІЗОВАНИЙ ЛОКАЛЬНИЙ ФРИЗ ЧАСУ (БЕЗ ЛАГІВ ТА ВИДАЛЕННЯ НЕБА)
task.spawn(function()
    while task.wait(0.05) do
        if _G.LocalVisualTime == "Day" then Lighting.ClockTime = 14
        elseif _G.LocalVisualTime == "Night" then Lighting.ClockTime = 0 end
    end
end)

local function getClosestChest()
    local closest, dist = nil, math.huge local Root = Player.Character and Player.Character:FindFirstChild("HumanoidRootPart") if not Root then return nil end
    for _, obj in pairs(workspace:GetDescendants()) do
        if obj:IsA("TouchTransmitter") and obj.Parent and string.find(obj.Parent.Name, "Chest") then
            local chest = obj.Parent local part = chest:IsA("BasePart") and chest or chest:FindFirstChildOfClass("BasePart")
            if part and (Root.Position - part.Position).Magnitude < dist then closest = part; dist = (Root.Position - part.Position).Magnitude end
        end
    end return closest
end
-- АВТОФАРМ СКРИНЬ (ПЛАВНИЙ ЦИКЛ БЕЗ КІКІВ)
task.spawn(function()
    while ScreenGui.Parent do
        task.wait(0.1)
        if _G.ChestFarm then
            local Root = Player.Character and Player.Character:FindFirstChild("HumanoidRootPart") 
            local chest = getClosestChest()
            if chest and Root and _G.ChestFarm then
                stopMovement() 
                currentBV = Instance.new("BodyVelocity", Root) 
                currentBV.Velocity = Vector3.new(0,0,0) 
                currentBV.MaxForce = Vector3.new(9e9, 9e9, 9e9)
                
                currentTween = game:GetService("TweenService"):Create(Root, TweenInfo.new((Root.Position - chest.Position).Magnitude / 350, Enum.EasingStyle.Linear), {CFrame = chest.CFrame})
                currentTween:Play() 
                
                local completed = false 
                local cConn = currentTween.Completed:Connect(function() completed = true end)
                while not completed and _G.ChestFarm do task.wait(0.01) end 
                cConn:Disconnect() 
                
                stopMovement() 
                if _G.ChestFarm then task.wait(0.15) end
            end
        end
    end
end)
-- КОНТРОЛЕР ПЛАВНОГО ПОЛЬОТУ
task.spawn(function()
    local flyBV, flyBG local UIS = game:GetService("UserInputService")
    while ScreenGui.Parent do
        task.wait() local Character = Player.Character local Root = Character and Character:FindFirstChild("HumanoidRootPart") local Humanoid = Character and Character:FindFirstChild("Humanoid") local Camera = workspace.CurrentCamera
        if _G.FlyMode and Root and Humanoid and Camera then
            if not flyBV then flyBV = Instance.new("BodyVelocity", Root) flyBV.Name = "XenoFlyVelocity" flyBV.MaxForce = Vector3.new(9e9, 9e9, 9e9) flyBG = Instance.new("BodyGyro", Root) flyBG.Name = "XenoFlyGyro" flyBG.MaxTorque = Vector3.new(9e9, 9e9, 9e9) end
            flyBG.CFrame = Camera.CFrame local direction = Humanoid.MoveDirection local velocity = direction * _G.FlySpeed
            if UIS:IsKeyDown(Enum.KeyCode.Space) then velocity = velocity + Vector3.new(0, _G.FlySpeed, 0) elseif UIS:IsKeyDown(Enum.KeyCode.LeftShift) then velocity = velocity + Vector3.new(0, -_G.FlySpeed, 0) end
            if direction.Magnitude == 0 and not UIS:IsKeyDown(Enum.KeyCode.Space) and not UIS:IsKeyDown(Enum.KeyCode.LeftShift) then flyBV.Velocity = Vector3.new(0, 0.1, 0) else flyBV.Velocity = velocity end
        else if flyBV then flyBV:Destroy() flyBV = nil end if flyBG then flyBG:Destroy() flyBG = nil end end
    end
end)
-- ОПТИМІЗОВАНИЙ FRUIT ESP (БЕЗ ЛАГІВ ТА СТРИБКІВ ФПС)
FruitEspBtn.MouseButton1Click:Connect(function() 
    _G.FruitEsp = not _G.FruitEsp 
    FruitEspBtn.Text = "🍓 Fruit Detector: " .. (_G.FruitEsp and "ON" or "OFF") 
end)

task.spawn(function()
    while task.wait(1.5) do
        if _G.FruitEsp then
            for _, v in pairs(workspace:GetChildren()) do
                if (v:IsA("Model") or v:IsA("Tool")) and (string.find(v.Name, "Fruit") or string.find(string.lower(v.Name), "fruit")) then
                    local p = v:FindFirstChild("Handle") or v:FindFirstChildOfClass("MeshPart") or v:FindFirstChildOfClass("Part")
                    if p and not p:FindFirstChild("FruitXenoBillboard") then
                        local b = Instance.new("BillboardGui", p) b.Name = "FruitXenoBillboard" b.Size = UDim2.new(0, 120, 0, 40) b.AlwaysOnTop = true b.ExtentsOffset = Vector3.new(0, 3, 0)
                        local t = Instance.new("TextLabel", b) t.Size = UDim2.new(1, 0, 1, 0) t.Text = "🍓 [ФРУКТ НА ЗЕМЛІ]" t.TextColor3 = Color3.fromRGB(255, 0, 100) t.BackgroundTransparency = 1 t.TextSize = 13 t.Font = Enum.Font.SourceSansBold
                        local s = Instance.new("BoxHandleAdornment", p) s.Name = "FruitXenoGlow" s.Size = p.Size + Vector3.new(0.5, 0.5, 0.5) s.Color3 = Color3.fromRGB(255, 0, 100) s.AlwaysOnTop = true s.ZIndex = 4 s.Adornee = p
                    end
                end
            end
        else
            for _, v in pairs(workspace:GetDescendants()) do if v.Name == "FruitXenoBillboard" or v.Name == "FruitXenoGlow" then v:Destroy() end end
        end
    end
end)
-- ОБРОБНИКИ НАЛАШТУВАНЬ ТА АКТИВАЦІЇ КНОПОК МЕНЮ
SetSpeedBtn.MouseButton1Click:Connect(function() 
    local num = tonumber(SpeedInput.Text) 
    if num then 
        _G.FlySpeed = num 
        SetSpeedBtn.Text = "ОК!" 
        task.wait(0.7) 
        SetSpeedBtn.Text = "Зберегти" 
    else 
        SpeedInput.Text = "Помилка" 
    end 
end)

ChestBtn.MouseButton1Click:Connect(function() 
    _G.ChestFarm = not _G.ChestFarm 
    if _G.ChestFarm then 
        _G.FlyMode = false 
        FlyBtn.Text = "Fly Mode: OFF" 
    end 
    ChestBtn.Text = "Auto Chest: " .. (_G.ChestFarm and "ON" or "OFF") 
    if not _G.ChestFarm then stopMovement() end 
end)

LevelBtn.MouseButton1Click:Connect(function() 
    print("Автофарм рівня відключено за запитом розробника") 
end)

FlyBtn.MouseButton1Click:Connect(function() 
    _G.FlyMode = not _G.FlyMode 
    if _G.FlyMode then 
        _G.ChestFarm = false 
        ChestBtn.Text = "Auto Chest: OFF" 
        stopMovement() 
    end 
    FlyBtn.Text = "Fly Mode: " .. (_G.FlyMode and "ON" or "OFF") 
end)

ExitBtn.MouseButton1Click:Connect(function()
    _G.ChestFarm = false 
    _G.FlyMode = false 
    _G.LocalVisualTime = "Default" 
    _G.FruitEsp = false 
    stopMovement() 
    for _, v in pairs(workspace:GetDescendants()) do 
        if v.Name == "FruitXenoBillboard" or v.Name == "FruitXenoGlow" then 
            v:Destroy() 
        end 
    end 
    local Root = Player.Character and Player.Character:FindFirstChild("HumanoidRootPart")
    if Root then 
        if Root:FindFirstChild("XenoFlyVelocity") then Root.XenoFlyVelocity:Destroy() end 
        if Root:FindFirstChild("XenoFlyGyro") then Root.XenoFlyGyro:Destroy() end 
    end 
    ScreenGui:Destroy()
end)
